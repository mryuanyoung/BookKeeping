# BookKeeping API

This directory is reserved for the future Go backend service.

Do not add a `go.mod` or server entrypoint until the backend implementation starts. When the API takes shape, keep the service code here and put shared contracts in `packages/contracts` or `packages/schemas` if Web and Android both need to consume them.
